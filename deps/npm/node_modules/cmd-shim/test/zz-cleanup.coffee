test = require("tap").test
path = require("path")
fixtures = path.resolve(__dirname, "fixtures")
rimraf = require("rimraf")
test "cleanup", (t) ->
  rimraf fixtures, (er) ->
    throw er  if er
    t.pass "cleaned up"
    t.end()
    return

  return

