clone = require("clone")
module.exports = (options, defaults) ->
  options = options or {}
  Object.keys(defaults).forEach (key) ->
    options[key] = clone(defaults[key])  if typeof options[key] is "undefined"
    return

  options
