test = (obj) ->
  _json = undefined
  _ini = undefined
  json = cc.parse(_json = JSON.stringify(obj))
  ini = cc.parse(_ini = INI.stringify(obj))
  console.log _ini, _json
  assert.deepEqual json, ini
  return
cc = require("..")
INI = require("ini")
assert = require("assert")
test hello: true
