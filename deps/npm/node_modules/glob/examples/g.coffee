Glob = require("../").Glob
pattern = "test/a/**/[cg]/../[cg]"
console.log pattern
mg = new Glob(pattern,
  mark: true
  sync: true
, (er, matches) ->
  console.log "matches", matches
  return
)
console.log "after"
