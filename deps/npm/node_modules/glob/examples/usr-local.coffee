Glob = require("../").Glob
pattern = "{./*/*,/*,/usr/local/*}"
console.log pattern
mg = new Glob(pattern,
  mark: true
, (er, matches) ->
  console.log "matches", matches
  return
)
console.log "after"
