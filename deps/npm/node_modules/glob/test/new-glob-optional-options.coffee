Glob = require("../glob.js").Glob
test = require("tap").test
test "new glob, with cb, and no options", (t) ->
  new Glob(__filename, (er, results) ->
    throw er  if er
    t.same results, [__filename]
    t.end()
    return
  )
  return

