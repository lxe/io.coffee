# remove the fixtures
tap = require("tap")
rimraf = require("rimraf")
path = require("path")
tap.test "cleanup fixtures", (t) ->
  rimraf path.resolve(__dirname, "a"), (er) ->
    t.ifError er, "removed"
    t.end()
    return

  return

