init = require("../init-package-json.js")
path = require("path")
dir = process.cwd()
initFile = require.resolve("./init/basic-init.js")
init dir, initFile, (err, data) ->
  console.log "written successfully"  unless err
  return

