init = require("../init-package-json.js")
path = require("path")
dir = process.cwd()
npm = require("npm")
npm.load (er, npm) ->
  throw er  if er
  init dir, npm.config.get("init-module"), npm.config, (er, data) ->
    throw er  if er
    console.log "written successfully"
    return

  return

