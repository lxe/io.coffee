pz = require("../promzard")
path = require("path")
file = path.resolve(__dirname, "substack-input.js")
ctx = basename: path.basename(path.dirname(file))
pz file, ctx, (er, res) ->
  throw er  if er
  console.error JSON.stringify(res, null, 2)
  return

