parse = require("../")
test = require("tape")
test "parse with modifier functions", (t) ->
  t.plan 1
  argv = parse([
    "-b"
    "123"
  ],
    boolean: "b"
  )
  t.deepEqual argv,
    b: true
    _: ["123"]

  return

