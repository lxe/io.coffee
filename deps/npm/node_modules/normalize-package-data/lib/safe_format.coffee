util = require("util")
module.exports = ->
  args = Array::slice.call(arguments, 0)
  args.forEach (arg) ->
    throw new TypeError("Bad arguments.")  unless arg
    return

  util.format.apply null, arguments
