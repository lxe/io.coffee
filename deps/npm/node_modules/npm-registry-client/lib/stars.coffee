stars = (base, name, cb) ->
  name = encodeURIComponent(name)
  path = "/-/_view/starredByUser?key=\"" + name + "\""
  @request "GET", url.resolve(base, path), null, cb
  return
url = require("url")
module.exports = stars
