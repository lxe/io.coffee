tap = require("tap")
rimraf = require("rimraf")
tap.test "setup", (t) ->
  rimraf __dirname + "/fixtures/cache", (er) ->
    throw er  if er
    t.pass "cache cleaned"
    t.end()
    return

  return

