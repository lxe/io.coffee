test = require("tap").test
readdir = require("../readdir.js")
test "basic", (t) ->
  
  # should not get {a,b}/{x,y}, but SHOULD get @org/ and @scope children
  expect = [
    "@org/x"
    "@org/y"
    "@scope/x"
    "@scope/y"
    "a"
    "b"
  ]
  readdir __dirname + "/fixtures", (er, kids) ->
    throw er  if er
    t.same kids, expect
    t.end()
    return

  return

