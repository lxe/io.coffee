#
# * Test several conditions that should always cause us to throw.
# 
test = ->
  ii = undefined
  ii = 0
  while ii < cases.length
    mod_assert.throws (->
      mod_ctype.parseCTF cases[ii].json
      return
    ), Error, cases[ii].msg
    ii++
  return
mod_assert = require("assert")
mod_ctype = require("../../ctype.js")
cases = [
  {
    json: {}
    msg: "bad JSON - no metadata or data"
  }
  {
    json:
      metadata: {}

    msg: "bad JSON - bad metadata section"
  }
  {
    json:
      metadata:
        "JSON version": []

    msg: "bad JSON - bad JSON version"
  }
  {
    json:
      metadata:
        "JSON version": 2

    msg: "bad JSON - bad JSON version"
  }
  {
    json:
      metadata:
        "JSON version": "100.20"

    msg: "bad JSON - bad JSON version"
  }
  {
    json:
      metadata:
        "JSON version": "1.0"

    msg: "missing data section"
  }
  {
    json:
      metadata:
        "JSON version": "1.0"

      data: 1

    msg: "invalid data section"
  }
  {
    json:
      metadata:
        "JSON version": "1.0"

      data: 1.1

    msg: "invalid data section"
  }
  {
    json:
      metadata:
        "JSON version": "1.0"

      data: "1.1"

    msg: "invalid data section"
  }
  {
    json:
      metadata:
        "JSON version": "1.0"

      data: {}

    msg: "invalid data section"
  }
]
test()
