
#
# * This is too unwieldly to actually write out. Just make sure we can parse it
# * without errrors.
# 
test = ->
  data = undefined
  data = JSON.parse(mod_fs.readFileSync("./psinfo.json").toString())
  mod_ctype.parseCTF data,
    endian: "big"

  return
mod_fs = require("fs")
mod_ctype = require("../../ctype.js")
mod_assert = require("assert")
test()
