
#jshint unused:false 
Store = ->
"use strict"
exports.Store = Store

# Stores may be synchronous, but are still required to use a
# Continuation-Passing Style API.  The CookieJar itself will expose a "*Sync"
# API that converts from synchronous-callbacks to imperative style.
Store::synchronous = false
Store::findCookie = (domain, path, key, cb) ->
  throw new Error("findCookie is not implemented")return

Store::findCookies = (domain, path, cb) ->
  throw new Error("findCookies is not implemented")return

Store::putCookie = (cookie, cb) ->
  throw new Error("putCookie is not implemented")return

Store::updateCookie = (oldCookie, newCookie, cb) ->
  
  # recommended default implementation:
  # return this.putCookie(newCookie, cb);
  throw new Error("updateCookie is not implemented")return

Store::removeCookie = (domain, path, key, cb) ->
  throw new Error("removeCookie is not implemented")return

Store::removeCookies = removeCookies = (domain, path, cb) ->
  throw new Error("removeCookies is not implemented")return
