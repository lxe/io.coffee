tar = require("../tar.js")
fs = require("fs")
fs.createReadStream(__dirname + "/../test/fixtures/c.tar").pipe(tar.Extract(path: __dirname + "/extract")).on("error", (er) ->
  console.error "error here"
  return
).on "end", ->
  console.error "done"
  return

